{% extends 'security/auth_base.html.twig' %}

{% block title %}Inscription | MediLink{% endblock %}

{% block content %}
  <div class="auth-head">
    <h1><i class="fa-solid fa-user-plus"></i> Créer un compte</h1>
  </div>

  <div class="auth-body">
    {{ form_start(registrationForm) }}

      <div class="form-group">
        {{ form_label(registrationForm.fullName) }}
        {{ form_widget(registrationForm.fullName, { attr: { class: 'input', placeholder: 'Ex : Mohamed Zribi' } }) }}
        {{ form_errors(registrationForm.fullName) }}
      </div>

      <div class="form-group">
        {{ form_label(registrationForm.email) }}
        {{ form_widget(registrationForm.email, { attr: { class: 'input', placeholder: 'ex : user@medilink.tn' } }) }}
        {{ form_errors(registrationForm.email) }}
      </div>

      <div class="form-group">
        {{ form_label(registrationForm.plainPassword) }}
        {{ form_widget(registrationForm.plainPassword, { attr: { class: 'input', placeholder: 'Minimum 6 caractères' } }) }}
        {{ form_errors(registrationForm.plainPassword) }}
      </div>

      <div style="margin-top: 16px;">
        <button class="btn-primary" type="submit">
          <i class="fa-solid fa-user-plus"></i> Créer mon compte
        </button>
      </div>

      <p class="hint" style="margin-top: 14px;">
        Déjà un compte ? <a class="link" href="{{ path('app_login') }}">Se connecter</a>
      </p>

    {{ form_end(registrationForm) }}
  </div>
{% endblock %}
